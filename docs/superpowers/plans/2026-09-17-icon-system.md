# 图标系统实施与验收

依据已确认的设计规范，使用本地 SVG 字典和显式占位符替换 UI 字符图标。动态内容由统一渲染器处理，不改写用户文本；天气码映射独立于户外风险筛选。

实施顺序：图标字典与天气映射 → 页面模板与状态更新 → 图标尺寸及点击区域 → 天气映射测试和业务回归 → 四种屏宽、弹窗和动态状态检查 → 原 GitHub Pages 域名发布并核对资源。

检查范围：发现、详情、收藏、手账、分享、计划、空状态与全局导航。文本路线分隔符、计算乘号和浏览器覆盖按钮不作为网站图标替换。

# P0 易用性实施计划

目标：按用户已同意的评价建议完成三项 P0，不扩展账户、地图 API 或实时票务。

设计：草案优先展示，地点核实按站合并，交通按实际路段集中补录；未知数据保持待核实。全天预算覆盖清单所有地点（含待安排）、额外餐饮及全天交通预留，不自动删除地点。冲突提供精确单站限制或组合冲突，并在完整重算成功后才展示可应用调整。

技术：保持静态 JavaScript、localStorage 和原 GitHub Pages 域名。新增 planning-assistant.js 纯函数负责预算、精简建议与冲突方案，planning-assistant-ui.js 负责表单和调整预览。复用现有排程器，不改营业和固定场次硬约束。

- [x] tests/usability.test.cjs：预算总额与缺项、0元、非法费用、精简清单、地点核实合并；返程超时、固定场次、缺失交通及建议重算测试，先运行观察失败。
- [x] 实现纯函数；所有建议必须保留已排入地点并包含目标站，固定场次不缩短。预算调整预览后应用并可撤销。
- [x] 界面先展示草案摘要及时间轴，将出发核实列表折叠并合并到每站一个按钮；常规设置折叠，未知开放不填假数据。
- [x] 添加人均全天预算卡、超额提示及精简/低价替换预览。交通批量表单一次保存，多次核实不重复选择路段。
- [x] 浏览器验证预算、冲突应用/撤销、手机排版与持久化；运行全部 Node 测试及语法检查。
- [x] 原域名部署，比对线上关键资源并更新 README。

验证结果：44 项 Node 测试通过，桌面及 375px 手机布局通过；预算精简、低价替换、应用与撤销、冲突调整、批量交通与刷新恢复均通过浏览器验证。be5bf8e 的 7 个关键静态资源在原域名返回 200，内容与本地一致。
